#!/usr/bin/env bash
# Pre-commit hook: regenerate articles/index.json and stage it.
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
"$REPO_ROOT/scripts/gen-index.sh"
git add "$REPO_ROOT/articles/index.json"
